CREATE TABLE IF NOT EXISTS courses (
    year INTEGER,
    study_period INTEGER,
    course_code VARCHAR(6),
    hp REAL,
    name TEXT,
    registered INTEGER,
    performance_rate REAL,
    eval_index REAL,
    answer_rate REAL
);
